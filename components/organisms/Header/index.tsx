import { Box } from "@mui/material";

// Components
import { Menu } from "../../molecules/Menu";

export const Header = () => {
  return (
    <Box component="header">
      <Menu />
    </Box>
  );
};
